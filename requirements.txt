# Django Core and Database
Django>=5.1,<6.0
psycopg2-binary>=2.9.0

# Django REST Framework
djangorestframework>=3.14.0

# CORS Headers
django-cors-headers>=4.0.0

# Country Fields
django-countries>=7.5.0

# Filtering
django-filter>=23.0

# Environment Variables
python-dotenv>=1.0.0

# Image Processing (for ImageField)
Pillow>=10.0.0

# === PRODUCTION ===
# WSGI HTTP Server (замість вбудованого runserver)
gunicorn>=21.2.0

# Статика на продакшні (fallback якщо Nginx не налаштований)
whitenoise>=6.5.0

# Admin UI (використовується в INSTALLED_APPS та admin.py)
django-unfold>=0.40.0

# HTTP requests (для верифікації Cloudflare Turnstile)
requests>=2.31.0

# Cloudflare R2 Media Storage
django-storages>=1.14.0
boto3>=1.34.0
